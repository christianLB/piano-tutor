# Copy to .env and adjust values
# Backend DB connection used inside backend container
BACKEND_DATABASE_URL=postgresql+psycopg://piano:piano_pass@db:5432/piano_tutor

# Postgres service credentials (compose)
POSTGRES_USER=piano
POSTGRES_PASSWORD=piano_pass
POSTGRES_DB=piano_tutor

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:8001
